dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install lame
    - sudo apt-get install fluidsynth

machine:
  environment:
    NODE_ENV: integration
  timezone:
    America/New_York
  node:
    version: v0.12.0

deployment:
  production:
    branch: master
    commands:
      - ./ci-deploy.sh
